# Database Migration Tool - Environment Configuration
# Copy this file to .env and fill in your actual credentials

# ============================================================================
# DATABASE CONNECTION (REQUIRED)
# ============================================================================

# Source Database (READ from)
READ_DB_HOST=localhost
READ_DB_PORT=3306
READ_DB_USER=root
READ_DB_PASSWORD=your_password_here

# Destination Database (WRITE to)
WRITE_DB_HOST=localhost
WRITE_DB_PORT=3306
WRITE_DB_USER=root
WRITE_DB_PASSWORD=your_password_here

# ============================================================================
# MIGRATION SETTINGS (OPTIONAL - defaults shown)
# ============================================================================

# Row threshold for confirmation prompts on reference tables
# Tables without customer_id exceeding this will prompt for confirmation
AUTO_CONFIRM_THRESHOLD=400

# Seed user IDs for user_id table filtering
SEED_USER_IDS=1,2

# Force-migrate tables (all data, no confirmation)
# Format: DATABASE.TABLE, *.TABLE, or TABLE
FORCE_MIGRATE_TABLES=FORCE_MIGRATE_TABLES=*.schema_version,*.flyway_schema_history,ATS.COUNTRY_CITY,ODIN.PATTERN_REACTION_V3,PLATFORM_SERVICE.COUNTRY_CITY,PLATFORM_SERVICE.TIMEZONES,USER_ACTION.REACTION_PATTERN

# Skip tables completely (never migrate)
# Format: DATABASE.TABLE, DATABASE.*, *.TABLE
SKIP_TABLES=DATAPROCESSOR.ATTACHMENT_FILE,ONBOARDING.CUSTOMER_V3,HIRE_INTEGRATIONS.JOB_DETAILS_FILE_METADATA


# Auto-skip large reference tables (default: false)
# When true, reference tables > AUTO_CONFIRM_THRESHOLD are auto-skipped
SKIP_LARGE_TABLES=true

# State file directory for tracking migration progress
MIGRATION_STATE_DIR=.migration_state
